<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="apackage.test.com.testsdk">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AchievementsActivity"/>
        <activity android:name=".ScreenshotActivity"/>
        <activity android:name=".LeaderboardActivity"/>

        <meta-data
            android:name="com.arioclub.android.sdk.APP_ID"
            android:value="@string/app_id" />
        <meta-data
            android:name="com.arioclub.android.sdk.KEY"
            android:value="@string/app_key" />

    </application>


</manifest>